version: '2'
services:
  library-postgresql:
    image: postgres:11.6
    # volumes:
    #     - ~/volumes/jhipster/library/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=library
      - POSTGRES_PASSWORD=
    ports:
      - 5432:5432
